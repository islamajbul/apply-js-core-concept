 function hourToMinute(hour){
    minute = hour*60;
    return minute;
}

const inputHour = 8;
const outputMinute = hourToMinute(inputHour);
console.log('Output minute-',  outputMinute);

/* function hourToMinute(hour){
    const minute = hour*60;
    return minute;
}

const inputHour = 30;
const outputMinute = hourToMinute(inputHour);
console.log('Output minute-',outputMinute); */